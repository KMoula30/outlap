{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Tyr",
  "description": "A tire model.",
  "type": "object",
  "properties": {
    "brush": {
      "description": "Optional physical brush-model block (`tyr/1.1`, §7.1). When present, T0 may build a brush\ntire from it; when the MF6.1 force core is also complete, both models are available and the\ntier picks MF6.1. A `brush` block with only a partial MF6.1 force set is a warning, not an\nerror — see [`crate::load::semantic::check_tyr`].",
      "anyOf": [
        {
          "$ref": "#/$defs/TyrBrush"
        },
        {
          "type": "null"
        }
      ]
    },
    "mf61": {
      "description": "MF6.1 coefficient block (superset of `.tir`, round-trippable).",
      "$ref": "#/$defs/Mf61Coeffs"
    },
    "provenance": {
      "description": "Provenance / citation.",
      "$ref": "#/$defs/TyrProvenance"
    },
    "schema": {
      "description": "Schema version, e.g. `tyr/1.0` (the `brush` block below requires `tyr/1.1`).",
      "$ref": "#/$defs/SchemaVersion"
    },
    "thermal": {
      "description": "Thermal-ring parameters (§7.2).",
      "$ref": "#/$defs/TyrThermal"
    },
    "vertical": {
      "description": "Optional structured tyre **vertical** dynamics (`tyr/1.2`, T3): the carcass vertical\nspring/damper the 14-DOF chassis uses to produce per-wheel `F_z` from the unsprung\ndeflection. This is the preferred home for the vertical stiffness over the raw\n`VERTICAL_STIFFNESS` MF6.1 map key (which stays supported as a fallback for back-compat);\nboth tiers' carcass-heat model already read the stiffness with a 250 kN/m fallback.",
      "anyOf": [
        {
          "$ref": "#/$defs/TyrVertical"
        },
        {
          "type": "null"
        }
      ]
    },
    "wear": {
      "description": "Wear/cliff parameters (§7.3).",
      "$ref": "#/$defs/TyrWear"
    }
  },
  "required": [
    "schema",
    "mf61",
    "thermal",
    "wear",
    "provenance"
  ],
  "$defs": {
    "BrushPressureProfile": {
      "description": "The contact-pressure distribution assumed by the brush model along the contact length.",
      "oneOf": [
        {
          "description": "Parabolic pressure `p(x) ∝ 1 − (x/a)²` — the classic brush assumption (the only option).",
          "type": "string",
          "const": "parabolic"
        }
      ]
    },
    "Mf61Coeffs": {
      "description": "MF6.1 (Pacejka 2012) coefficients keyed by their standard `.tir` names, e.g. `PCX1`, `PKY1`.\n\nValidated in the semantic stage: unknown coefficient names produce a did-you-mean warning\n(the spec is silent on the full set, so unknown keys are non-fatal); the [`REQUIRED_STRUCTURAL_KEYS`]\nare always mandatory, and the [`REQUIRED_FORCE_KEYS`] are mandatory unless a [`TyrBrush`] block\nsupplies the force model instead.",
      "type": "object",
      "additionalProperties": {
        "type": "number",
        "format": "double"
      }
    },
    "SchemaVersion": {
      "description": "Schema version of the form <name>/<MAJOR>.<MINOR>, e.g. vehicle/1.0",
      "type": "string",
      "examples": [
        "vehicle/1.0"
      ],
      "pattern": "^[a-z_]+/\\d+\\.\\d+$"
    },
    "TyrBrush": {
      "description": "Physical brush-model parameters (Pacejka 2012, ch. 3): a first-principles alternative to the\nempirical MF6.1 force core, parameterised by tread stiffnesses, a base friction, and the\ncontact half-length. Introduced at `tyr/1.1`.",
      "type": "object",
      "properties": {
        "c_alpha_n_per_rad": {
          "description": "Lateral (cornering) tread stiffness `C_α`, N/rad (force per unit slip-angle at the origin).",
          "type": "number",
          "format": "double"
        },
        "c_kappa_n": {
          "description": "Longitudinal tread stiffness `C_κ`, N (force per unit slip-ratio at the origin).",
          "type": "number",
          "format": "double"
        },
        "mu0": {
          "description": "Base sliding friction coefficient `μ0` (dimensionless; scaled at runtime by `mu_scale_*`).",
          "type": "number",
          "format": "double"
        },
        "patch_half_length_m": {
          "description": "Contact-patch half-length `a`, m (sets the closed-form pneumatic trail `t(0) = a/3`).",
          "type": "number",
          "format": "double"
        },
        "pressure_profile": {
          "description": "Contact-pressure profile along the patch. Only the classic parabolic profile is modelled.",
          "$ref": "#/$defs/BrushPressureProfile",
          "default": "parabolic"
        }
      },
      "required": [
        "c_kappa_n",
        "c_alpha_n_per_rad",
        "mu0",
        "patch_half_length_m"
      ]
    },
    "TyrProvenance": {
      "description": "Provenance / citation for a tire dataset.",
      "type": "object",
      "properties": {
        "citation": {
          "description": "Literature citation the coefficients derive from.",
          "type": "string"
        },
        "source": {
          "description": "Human-readable source note.",
          "type": "string"
        },
        "synthetic": {
          "description": "Whether the dataset is synthetic (physically plausible but not measured).",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "citation",
        "source"
      ]
    },
    "TyrThermal": {
      "description": "Two-node (tread surface / carcass, plus inflation gas) thermal-ring parameters (§7.2).",
      "type": "object",
      "properties": {
        "c_c": {
          "description": "Carcass/bulk node thermal capacity, J/K.",
          "type": "number",
          "format": "double"
        },
        "c_g": {
          "description": "Inflation-gas node thermal capacity, J/K.",
          "type": "number",
          "format": "double"
        },
        "c_s": {
          "description": "Tread-surface node thermal capacity, J/K.",
          "type": "number",
          "format": "double"
        },
        "c_t": {
          "description": "Grip-window width coefficient `c_T`.",
          "type": "number",
          "format": "double"
        },
        "g_cg": {
          "description": "Carcass↔gas conductance, W/K.",
          "type": "number",
          "format": "double"
        },
        "g_road": {
          "description": "Carcass↔road conductance, W/K.",
          "type": "number",
          "format": "double"
        },
        "g_sc": {
          "description": "Surface↔carcass conductance, W/K.",
          "type": "number",
          "format": "double"
        },
        "h0": {
          "description": "Convection intercept `h0` in `h(v) = h0 + h1·v^0.8`, W/(m²·K).",
          "type": "number",
          "format": "double"
        },
        "h1": {
          "description": "Convection slope `h1`, W/(m²·K) per (m/s)^0.8.",
          "type": "number",
          "format": "double"
        },
        "k_c": {
          "description": "Carcass-softening coefficient `k_c`.",
          "type": "number",
          "format": "double"
        },
        "p_cold": {
          "description": "Cold inflation pressure `p_cold`, kPa.",
          "type": "number",
          "format": "double"
        },
        "p_t": {
          "description": "Friction-power partition into the surface node, 0..1 (≈0.6–0.7).",
          "type": "number",
          "format": "double"
        },
        "t_c_ref": {
          "description": "Carcass-softening reference temperature `T_c,ref`, °C.",
          "type": "number",
          "format": "double"
        },
        "t_cold": {
          "description": "Cold reference temperature `T_cold`, °C.",
          "type": "number",
          "format": "double"
        },
        "t_opt": {
          "description": "Optimal grip temperature `T_opt`, °C.",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "c_s",
        "c_c",
        "c_g",
        "g_sc",
        "g_cg",
        "g_road",
        "h0",
        "h1",
        "p_t",
        "t_opt",
        "c_t",
        "k_c",
        "t_c_ref",
        "p_cold",
        "t_cold"
      ]
    },
    "TyrVertical": {
      "description": "Structured tyre **vertical** dynamics (`tyr/1.2`, T3; Pacejka 2012 ch. 1). The tyre carcass acts\nas a vertical spring — and optionally a light damper — between the unsprung mass and the road,\nproducing the per-wheel normal load `F_z` at T3. Preferred over the raw `VERTICAL_STIFFNESS`\nMF6.1 coefficient (which stays supported as a fallback so existing `.tyr` files keep working).",
      "type": "object",
      "properties": {
        "damping_n_s_per_m": {
          "description": "Tyre vertical damping `c_z`, N·s/m (≥ 0). Optional; small — tyres are lightly damped.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "stiffness_n_per_m": {
          "description": "Tyre vertical stiffness `k_z`, N/m (> 0). Racing-slick-representative ≈ 250 kN/m.",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "stiffness_n_per_m"
      ]
    },
    "TyrWear": {
      "description": "Wear and thermal-damage (cliff) parameters (§7.3).",
      "type": "object",
      "properties": {
        "beta": {
          "description": "Damage exponent `β`.",
          "type": "number",
          "format": "double"
        },
        "delta_c": {
          "description": "Cliff grip drop `Δ_c`, fraction.",
          "type": "number",
          "format": "double"
        },
        "delta_d": {
          "description": "Thermal-damage grip drop `Δ_D`, fraction.",
          "type": "number",
          "format": "double"
        },
        "delta_t_ref": {
          "description": "Damage reference temperature step `ΔT_ref`, K.",
          "type": "number",
          "format": "double"
        },
        "k_w": {
          "description": "Wear coefficient `k_w`.",
          "type": "number",
          "format": "double"
        },
        "s_w": {
          "description": "Cliff sharpness `s_w`, mm.",
          "type": "number",
          "format": "double"
        },
        "t_deg": {
          "description": "Damage-onset temperature `T_deg`, °C.",
          "type": "number",
          "format": "double"
        },
        "tau_d": {
          "description": "Thermal-damage time constant `τ_D`, s.",
          "type": "number",
          "format": "double"
        },
        "w_c": {
          "description": "Cliff onset tread depth `w_c`, mm.",
          "type": "number",
          "format": "double"
        },
        "w_max": {
          "description": "New-tread depth `w_max`, mm.",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "k_w",
        "w_max",
        "w_c",
        "tau_d",
        "t_deg",
        "delta_t_ref",
        "beta",
        "delta_c",
        "s_w",
        "delta_d"
      ]
    }
  }
}
