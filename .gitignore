target/
__pycache__/
.venv/
*.egg-info/
dist/
.pytest_cache/
# proptest failure seeds are machine-local, not committed in this repo.
*.proptest-regressions

# Reference literature (copyrighted — used for clean-room derivation, never redistributed).
# Coefficient VALUES are facts we may use and cite; the source PDFs are not committed.
**/*.pdf

# Local FSAE TTC test data (membership-locked, NON-redistributable). The tirefit parsers read
# from here; neither raw TTC files nor TTC-derived fitted parameter sets are ever committed.
ttc-data/

# Real-PDT-derived artifacts stay local (firewall; M3 user decision #7): any `local/` dir under a
# vehicle holds imported drive-unit maps / packs, and the real-data twin of the notebook 07 capstone.
data/vehicles/*/local/
notebooks/07_qss_t1_local.ipynb
