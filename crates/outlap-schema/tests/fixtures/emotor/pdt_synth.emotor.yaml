# SPDX-License-Identifier: CC-BY-SA-4.0
# Synthetic PDT-distilled emotor (importer output on a synthetic 2-node fixture). NOT real PDT data.
# Imported by outlap.importers.pdt_h5 from e.h5 (§10.2 step 6)
# distilled 2-node thermal
schema: emotor/1.0
nodes:
  winding:
    c_j_per_k: 369.73
    t_max_c: 180.0
    t_warn_c: 150.0
  case:
    c_j_per_k: 3012.7
    t_max_c: 120.0
    t_warn_c: 105.0
coupling:
  g_wc_w_per_k: 139.2968
  g_cool_w_per_k: 196.7918
cooling:
  liquid:
    coolant_temp_c: 65.0
loss_routing:
  winding_split: 0.7
  copper_alpha_per_k: 0.00393
meta:
  source: pdt_distilled
  notes: Distilled from PDT 19-node LPTN (EDrive synth_2node unknown). Fit RMS 0.13%
    over 12 envelope points (cont + [10, 20, 30] s at 3 speeds). T_max/T_warn assumed
    (class-H defaults); case capacitance is weakly constrained by winding-limited
    envelopes; copper feedback on.
